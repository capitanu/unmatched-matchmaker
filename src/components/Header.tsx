import React from "react";

const Header: React.FC = () => (
  <header className="header">
    <h1 className="game-title">Unmatched</h1>
    <h2 className="app-name">Unmatched Matchmaker</h2>
  </header>
);

export default Header;
